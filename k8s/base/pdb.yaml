apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mutuelle-rag-agent
  labels:
    app: mutuelle-rag-agent
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: mutuelle-rag-agent
